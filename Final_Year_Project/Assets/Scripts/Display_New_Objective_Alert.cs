using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Display_New_Objective_Alert : MonoBehaviour
{
    /*
    // Start is called before the first frame update
    [SerializeField]
    private GameObject NewObjectAlert;
    [SerializeField]
    private Activate_Text Activate_Text;
    private bool DisableObjectiveAlert;
    void Start()
    {
        NewObjectAlert.SetActive(false);
        Activate_Text = FindObjectOfType<Activate_Text>();
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log(Activate_Text.GetComponent<Activate_Text>().DialogueFinished);
        if (Activate_Text.GetComponent<Activate_Text>().DialogueFinished == true && DisableObjectiveAlert == false)
        {
            DisplayObjectiveAlert();
            Invoke("HideObjectiveAlert", 3f);
        }
    }

    private void DisplayObjectiveAlert()
    {
        NewObjectAlert.SetActive(true);
    }

    private void HideObjectiveAlert()
    {
        NewObjectAlert.SetActive(false);
        DisableObjectiveAlert = true;
    }
    */
}
